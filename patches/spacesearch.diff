diff --git a/config.def.h b/config.def.h
index a221c86..c3aef5e 100644
--- a/config.def.h
+++ b/config.def.h
@@ -126,1 +126,3 @@ #define MODKEY GDK_CONTROL_MASK
+
+static char *searchengine = "https://duckduckgo.com/?q=";
 
 /* hotkeys */
diff --git a/surf.c b/surf.c
index cebd469..a5c0030 100644
--- a/surf.c
+++ b/surf.c
@@ -573,6 +573,8 @@ loaduri(Client *c, const Arg *arg) {
 	    g_str_has_prefix(uri, "file://")  ||
 	    g_str_has_prefix(uri, "about:")) {
 		url = g_strdup(uri);
+	} else if (*uri == ' ') {
+		url = g_strdup_printf("%s%s", searchengine, uri+1);
 	} else {
 		if (uri[0] == '~')
 			apath = untildepath(uri);
 
2.24.1

